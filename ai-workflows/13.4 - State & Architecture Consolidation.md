# Workflow 13.4 — State & Architecture Consolidation

Implement the architectural improvements identified during Workflow 12.3.

## Goals

Simplify the architecture.

Improve state ownership.

Reduce coupling.

Improve maintainability.

## Tasks

* consolidate parser state
* consolidate matcher state
* eliminate long parameter lists
* reduce multiple return values used only to transport execution state
* simplify public APIs
* simplify internal APIs
* improve module responsibilities
* remove unnecessary abstractions
* simplify state flow
* reduce coupling between modules
* improve consistency across the project

Preserve the AST architecture.

## Engineering Standards

Apply the same engineering standards established in previous workflows.

## Validation

Continuously verify:

* `cd tests && lua init.lua`
* `cd tests && lua test_matcher.lua`
* all affected tests

## Commit

Commit progress in logical batches.

Create a final commit summarizing the architectural consolidation.
